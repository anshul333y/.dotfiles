#!/bin/bash

sudo apt update && sudo apt upgrade -y

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

sudo apt autoremove -y

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

sudo snap refresh

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

flatpak update --noninteractive
